# NeetLogIQ SQLite Requirements
# Core dependencies for the SQLite-based data processing pipeline

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# Database
sqlite3  # Built-in with Python

# Machine Learning and Text Processing
scikit-learn>=1.3.0
rapidfuzz>=3.0.0

# Configuration
PyYAML>=6.0

# Progress bars and UI
tqdm>=4.65.0
inquirer>=3.1.0

# Parallel processing
concurrent.futures  # Built-in with Python
multiprocessing  # Built-in with Python

# Logging
logging  # Built-in with Python
structlog>=23.0.0  # Structured logging for machine-readable logs

# File handling
pathlib  # Built-in with Python
os  # Built-in with Python
re  # Built-in with Python
uuid  # Built-in with Python
datetime  # Built-in with Python
time  # Built-in with Python
sys  # Built-in with Python

# Optional: For advanced features
# streamlit>=1.25.0  # For web-based human feedback interface
# plotly>=5.15.0     # For data visualization
# jupyter>=1.0.0     # For interactive development

# NEW ADVANCED FEATURES (2025)
watchdog>=3.0.0       # For stream processing (directory watching)
cachetools>=5.3.0     # For proper LRU cache implementation
jellyfish>=1.0.0      # For phonetic matching (if not already installed)
